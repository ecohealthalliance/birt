if $primary is defined
  $default = $primary
else
  $default = rgba(100, 100, 100, 1)
  $primary = $default

$tabs-bg = alpha($primary, 80%)

$inactive-btn = $transparent-white
$active-btn = white

$sidebar-z-index ?= 2000

$sidebar-main-width ?= 350px

if $sidebar-main-width < 250
  $sidebar-main-width = 250px

$sidebar-tabular-width ?= 580px

$sidebar-main-menu-width ?= 50px

$content-bg ?= rgba(255, 255, 255, .9)
